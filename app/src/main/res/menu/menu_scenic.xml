<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <!--app:showAsAction
    ，取值有5种，主要应用的有ifRoom、never、always 这三种，
     ifRoom 表示 如果Toolbar 上有显示空间就显示在Toolbar 上，
     如果没有空间就展示在溢出菜单里，never 表是总是显示在溢出菜单里，
    always 表示总是显示在Toolbar 上。
    -->
    <item android:id="@+id/menu_scenic_add"
          android:icon="@mipmap/test_chat"
          android:title="晒出心情"
          app:showAsAction="never"/>

    <item android:id="@+id/menu_scenic_info"
          android:icon="@mipmap/bottom_menu_love_white"
          android:title="景区详情"
          app:showAsAction="never"/>

</menu>